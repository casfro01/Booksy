import { AuthorClient, BookClient, GenreClient } from "../LibAPI";

export const authorClient = new AuthorClient();
export const bookClient = new BookClient();
export const genreClient = new GenreClient();